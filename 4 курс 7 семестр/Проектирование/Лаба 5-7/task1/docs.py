USER_DOC = """    Пользовательская документация (П-документация)

1. Общее функциональное описание ПС
ПС предназначена для хранения и управления расписанием рейсов авиакомпании (БД «Аэрофлот»):
  - Создание, чтение, изменение, удаление записей рейсов.
  - Файл/БД должен содержать не менее 5 записей при первом запуске (инициализация).
  - Поиск по полям (номер рейса, пункт назначения, время).
  - Формирование отчёта за период (диапазон дат/времён).
  - Две преднастроенные формы отчётности (а также возможность подключить третью):
    - Отчёт 1: «Рейсы за период» (фильтр по дате/времени отправления) — таблица + экспорт CSV.
    - Отчёт 2: «Доступные места по пункту назначения» (группировка по пункту назначения).
    - Отчёт 3 (опционально подключаемая): «Сводка по времени суток» (утро/день/вечер/ночь).
  - Простой GUI (Tkinter) с формой ввода и списком.
  - Документация: модульная документация, пользовательская документация, встроенная справка.

2. Руководство по инсталляции
- Требования: Python 3.8+, библиотеки tkinter, sqlite3 (входит в стандартную дистрибуцию).
- Запуск: python main.py

3. Инструкция по применению
- Вкладка 'Flights': добавление/редактирование/удаление записей рейсов, поиск, экспорт CSV.

4. Справочник и управление
- Форматы дат: YYYY-MM-DD HH:MM для полей времени.
- При ошибках валидатор покажет сообщение с подсказкой к исправлению.
"""

DEV_DOC = """    Документация по сопровождению (С-документация)

1. Архитектура модулей:
main: точка входа, запускает GUI (gui.App)
  |_ gui: создание GUI и добавление функциональности для взаимодействия с пользователем
      |_ mio: экспорт/сохранение (CSV)
      |_ validation: функции валидации полей
      |_ data: хранение и CRUD (sqlite3)
      |_ docs: переменные, которые хранят документацию

2. Оценка сцепления и связанностей модулей:
- Связанность (coupling):
  gui <-> data — информационная: GUI вызывает функции data через API функции insert/update/query, передаёт параметры и получает результаты.
  gui <-> validation — информационная (validation — чистые функции).
  gui <-> mio — информационная: GUI вызывает функцию экспорта с передачей параметров
  data <-> validation — по совпадению (data не зависит от validation, только от валидных данных).
  data <-> mio — по совпадению, т.к. экспорт данных получает данные не напрямую из БД, а из таблицы GUI
  mio <-> validation — по совпадению, экспортирует данные, прошедшие валидацию

- Сцепление (cohesion):
  gui <-> data — сцепление по данным
  gui <-> validation — сцепление по данным
  gui <-> mio — сцепление по данным

3. Схема информационных связей:
  [Пользователь] -> ввод через GUI -> GUI передаёт данные -> validation -> (если OK) -> data.insert/update
  GUI <- data.query() <- Запросы на получение списка (фильтры, поиск)
  GUI -> Reports -> data.query() -> Reports формирует таблицу -> экспорт CSV

4. Схема HIPO:
- Система управления рейсами
  1) Инициализация
     Вход: (БД существует/отсутствует)
     Обработка: создает БД и вставляет данные при необходимости
     Выход: готовая БД
  2) CRUD
     Вход: данные с формы
     Обработка: валидация -> insert/update/delete в БД
     Выход: измененная БД, успешно/ошибка
  3) Поиск с фильтрацией
     Вход: фильтрующие параметры
     Обработка: создание SQL выражение -> получение данных
     Выход: таблица в приложении
  4) Отчеты
     Вход: параметры для создания отчета
     Обработка: запрос к БД -> агрегация -> форматирование
     Выход: таблица в приложении, эксорт в CSV
  5) Справка
     Вход: клик клиента
     Обработка: показать встроенные документы
     Выход: окна без возможности редактирования

5. Схема Варнье-Орра:
  ADD_FLIGHT
    INPUT: flight_record
      FLIGHT_NUMBER (string)
      DESTINATION (string)
      DEPARTURE_DATETIME (datetime)
      ARRIVAL_DATETIME (datetime)
      FREE_SEATS (int)
      PLANE_TYPE (string)
    VALIDATE
      IF invalid -> RETURN error
    INSERT -> DB
    RETURN success

6. Требования к данным (схема таблицы flights)
  id INTEGER PRIMARY KEY AUTOINCREMENT
  flight_no TEXT NOT NULL UNIQUE
  destination TEXT NOT NULL
  departure TEXT (ISO datetime) NOT NULL
  arrival TEXT (ISO datetime) NOT NULL
  free_seats INTEGER NOT NULL
  plane_type TEXT (опционально)
  notes TEXT (опционально)

7. Тестовые и отчетные требования
  Первичная инициализация БД содержит 5 записей.
  Отчёт «Рейсы за период» должен фильтровать по интервалу departure между start и end.
  Отчёт «Доступные места по направлению» должен агрегировать свободные места по destination.
  Функция поиска поддерживает фильтр по номеру рейса и/или пункту назначения (частичное совпадение).
"""
